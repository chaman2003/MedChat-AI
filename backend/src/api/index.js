/**
 * API Module Index
 * Exports all API components (routes, controllers, middleware)
 */
const routes = require("./routes");
const middleware = require("./middleware");

module.exports = {
  routes,
  middleware,
};
